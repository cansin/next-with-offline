{
  "name": "@next-with-offline",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.16.3"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna exec --parallel -- rollup -c",
    "dev": "lerna exec --parallel -- rollup -c -w",
    "test": "eslint . && lerna exec --parallel -- jest",
    "pretest": "yarn build"
  },
  "devDependencies": {
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "@rollup/plugin-babel": "^5.0.0",
    "babel-eslint": "^10.1.0",
    "babel-jest": "^26.0.1",
    "eslint": "^6.0.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-prettier": "^3.1.3",
    "jest": "^26.0.1",
    "lerna": "^3.20.2",
    "prettier": "^2.0.5",
    "rollup": "^2.9.0"
  }
}
